# Anticipa - Environment Variables Template
# Copy this file to .env and fill in your actual values

# =============================================================================
# SUPABASE AUTHENTICATION
# =============================================================================
# Get these from your Supabase project: https://app.supabase.com/project/_/settings/api

SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your-anon-key-here

# Database schema where auth table is located (default: public)
SUPABASE_SCHEMA=public

# Name of your authentication table (default: auth)
AUTH_TABLE=auth

# =============================================================================
# APPLICATION SETTINGS (now in app/app_config.py)
# =============================================================================
# ALLOW_SIGNUP - Moved to app/app_config.py for easier management
# Edit app/app_config.py to change signup settings

# =============================================================================
# DEPLOYMENT NOTES
# =============================================================================
# For Render deployment:
# 1. Go to your Render service -> Environment tab
# 2. Add each variable above as a separate environment variable
# 3. Do NOT include quotes around values in Render
# 4. Do NOT commit your actual .env file to git
#
# Required Environment Variables:
#   - SUPABASE_URL (required)
#   - SUPABASE_KEY (required)
#   - SUPABASE_SCHEMA (optional, default: public)
#   - AUTH_TABLE (optional, default: auth)
#
# For Streamlit Cloud:
# 1. Go to App settings -> Secrets
# 2. Add these as TOML format in secrets:
#    SUPABASE_URL = "your-url"
#    SUPABASE_KEY = "your-key"
